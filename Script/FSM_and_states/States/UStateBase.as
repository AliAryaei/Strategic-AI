
class UStateBase
{
    //Defines what happen when Agent enters the state. Returns void.
	void Enter(AEntity Entity)
    {

    }
	//Defines what happen when Agent Executes the state. Returns void.
	void Execute(AEntity Entity, float DeltaSeconds)
    {

    }
	//Defines what happen when Agent exits the state. Returns void.
	void Exit(AEntity Entity)
    {

    }

}